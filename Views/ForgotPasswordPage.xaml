<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiFrontend.Views.ForgotPasswordPage"
             Title="Khôi phục mật khẩu"
             BackgroundColor="#F7F9FC">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="25" VerticalOptions="CenterAndExpand">

            <Label Text="Khôi phục mật khẩu"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="#2C3E50"
                   Margin="0,0,0,10"/>

            <!-- 📨 Bước 1: Nhập Email -->
            <Frame HasShadow="False" CornerRadius="12" BackgroundColor="White" Padding="10"
                   IsVisible="{Binding IsEmailStep}">
                <VerticalStackLayout Spacing="10">
                    <Entry Placeholder="Nhập email"
                           Text="{Binding Email}"
                           Keyboard="Email"
                           FontSize="16"
                           TextColor="#2C3E50"
                           PlaceholderColor="#95A5A6"/>
                    <Button Text="Gửi mã OTP"
                            Command="{Binding SendOtpCommand}"
                            BackgroundColor="#3498DB"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="45"/>
                </VerticalStackLayout>
            </Frame>

            <!-- 🔢 Bước 2: Nhập mã OTP -->
            <Frame HasShadow="False" CornerRadius="12" BackgroundColor="White" Padding="10"
                   IsVisible="{Binding IsOtpStep}">
                <VerticalStackLayout Spacing="10">
                    <Entry Placeholder="Nhập mã xác nhận OTP"
                           Text="{Binding Otp}"
                           Keyboard="Numeric"
                           FontSize="16"
                           TextColor="#2C3E50"
                           PlaceholderColor="#95A5A6"/>
                    <Button Text="Xác nhận OTP"
                            Command="{Binding VerifyOtpCommand}"
                            BackgroundColor="#3498DB"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="45"/>
                </VerticalStackLayout>
            </Frame>

            <!-- 🔒 Bước 3: Nhập mật khẩu mới -->
            <Frame HasShadow="False" CornerRadius="12" BackgroundColor="White" Padding="10"
                   IsVisible="{Binding IsPasswordStep}">
                <VerticalStackLayout Spacing="10">
                    <Entry Placeholder="Mật khẩu mới"
                           IsPassword="True"
                           Text="{Binding NewPassword}"
                           FontSize="16"
                           TextColor="#2C3E50"
                           PlaceholderColor="#95A5A6"/>
                    <Button Text="Đặt lại mật khẩu"
                            Command="{Binding ResetPasswordCommand}"
                            BackgroundColor="#2ECC71"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="45"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Quay lại đăng nhập -->
            <Button Text="← Quay lại đăng nhập"
                    Command="{Binding BackToLoginCommand}"
                    BackgroundColor="Transparent"
                    TextColor="#3498DB"
                    FontSize="14"
                    HorizontalOptions="Center"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
